# Authorize.Net 

It is an API that is used as an intermediate API when someone wants to book a cab or any other commute to one of event's destination.
In an event of failure or suspicious usage of Online Banking(read: Fraud Detection), Authorize.API will flag an alert about the same.
